nelmio_api_doc:
    documentation:
        info:
            title: Jeu de Cartes API
            description: API pour gérer un jeu de cartes
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
